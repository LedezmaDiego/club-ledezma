services:
  - type: web
    name: club-ledezma
    env: python
    branch: main
    rootDirectory: backend
    region: oregon
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    plan: free
